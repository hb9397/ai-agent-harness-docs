# 병렬 실행 환경 설정 (parallel-setup)
# 역할: 플랫폼 감지 → 실행 방식 선택 → 서브에이전트 모델 확정

---

## 라우팅 표

| 호출 지점 | 실행할 섹션 순서 |
|----------|----------------|
| STEP 0 — 고정 Task 스킬 (multi-review, doc-audit, context-doc) | [플랫폼 확인] → [모델 목록 표시] → [실행 방식 선택] → [모델 확정] |
| STEP 0 — 동적 Task 스킬 (create-prototype, code-comment, impl-fe-be-doc, impl-doc, impl-reuse-scan, impl-verify) | [플랫폼 확인] → [모델 목록 표시] → [실행 방식 선택 — 선호도만 저장] |
| Task 목록 확정 후 — 동적 Task 스킬 | [모델 확정] |

---

## [플랫폼 확인]

> "현재 어떤 플랫폼에서 실행 중인가요?
> 1) Claude Code  2) OpenAI Codex CLI  3) Gemini CLI  4) 기타 (순차로 진행)"

- 4) 기타 선택 시 → 서브에이전트 없이 순차 실행. SKILL.md의 다음 Step으로 바로 진행.
- 1)~3) 선택 시 → [모델 목록 표시]로 이동.

---

## [모델 목록 표시]

선택한 플랫폼에서 **현재 사용 가능한 모델 목록**을 조회하여 표로 제시한다.
모델명은 빠르게 변하므로 하드코딩하지 않는다.

**조회 방법 (플랫폼별):**
- Claude Code: `/model` 명령 또는 설정 확인
- OpenAI Codex CLI: `models list` 또는 설정 확인
- Gemini CLI: `models list` 또는 설정 확인

**표시 기준:**
| 등급 | 용도 | 선택 기준 |
|------|------|----------|
| 경량 모델 | 서브에이전트 기본 추천 | 가장 빠르고 저렴한 모델 |
| 균형 모델 | 복잡한 작업 | 성능-비용 균형 |
| 고성능 모델 | 최고 품질 필요 시 | 비용 높음, 필요시만 |

조회 불가 시 사용자에게 직접 모델명 입력을 요청한다.

---

## [실행 방식 선택]

> "병렬(서브에이전트)로 실행할까요, 순차로 실행할까요?
> - 병렬: 빠르지만 비용 발생
> - 순차: 느리지만 추가 비용 없음"

- 순차 선택 시 → SKILL.md의 다음 Step으로 진행. 모델 확정 불필요.
- 병렬 선택 시 → [모델 확정]으로 이동.

**동적 Task 스킬 (STEP 0에서 호출):**
- 병렬 선호 시 → 선호도와 플랫폼만 저장. 모델 확정은 Task 목록 확정 후 별도로 진행.
- 순차 선택 시 → SKILL.md의 다음 Step으로 진행.

---

## [모델 확정]

SKILL.md에서 전달받은 병렬 Task 목록을 표시한 뒤 묻는다.

> "서브에이전트 모델을 설정하겠습니다.
> 위 {N}개 Task에 모델을 어떻게 적용할까요?
> 1) 전체 Task에 같은 모델 적용
> 2) Task별로 모델 지정"

- 1) 선택 시 → 사용할 모델 1개 확인. 확정 후 SKILL.md 다음 Step으로 진행.
- 2) 선택 시 → 각 Task별로 모델을 순서대로 수집. 전부 확정 후 SKILL.md 다음 Step으로 진행.
