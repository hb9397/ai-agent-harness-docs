# Harness Plugin Maintainer Fixtures

Phase 6 evals build from the live repository source because the plugin payload is the source-of-truth user skill set. Fixture-specific failure cases are covered by `validate_plugin.py` checks.
