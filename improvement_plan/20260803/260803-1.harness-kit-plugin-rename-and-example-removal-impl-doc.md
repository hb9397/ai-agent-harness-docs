# 구현 작업지침서: Harness Kit 플러그인 식별자 전환과 example 외부화

> 생성 스킬: impl-doc + brainstorming 승인 설계
> 프로젝트 유형: AI Agent 플러그인 관리 저장소
> 작성일: 2026-08-03
> 작성자/갱신자: hb9397
> 상태: 설계 승인 / 구현 대기
> 적용 대상: `C:/Plugin_Workspace/harness-kit` 단일 저장소와 외부 examples 저장소
> 기준 커밋: `a35749249284eadf6369757741ab58bb7668c843` (`v0.2.2`)
> 목표 릴리스: `0.3.0`

---

## 1. 목적과 결정 사항

이 계획은 두 개의 독립적인 파괴적 변경을 안전하게 수행하기 위한 작업 지침이다.

1. 현재 `ai-agent-harness` 플러그인의 설치 정체성을 `harness-kit`으로 변경한다.
2. marketplace 식별자를 `ai-agent-harness`에서 `hb9397`로 변경한다.
3. Git remote와 plugin provenance의 repository URL을 `hb9397/harness-kit`으로 정합화한다.
4. 새 로컬 checkout 기준 경로 `C:/Plugin_Workspace/harness-kit`을 사용하고
   machine-specific 절대 경로를 생성 metadata에서 제거한다.
5. 루트 보호 자산 `example/`을 외부 examples 저장소로 이관한 뒤 현재 저장소에서 제거한다.

최종 설치 표면은 다음과 같다.

| 표면 | 현재 | 목표 | 비고 |
|---|---|---|---|
| plugin ID/name | `ai-agent-harness` | `harness-kit` | Codex·Claude manifest 공통 |
| marketplace name | `ai-agent-harness` | `hb9397` | 소유자 namespace와 일치 |
| qualified install | `ai-agent-harness@ai-agent-harness` | `harness-kit@hb9397` | 새 설치·업데이트 기준 |
| display name | `AI Agent Harness` | `Harness Kit` | 사용자에게 보이는 이름 |
| plugin root | `plugins/ai-agent-harness/` | `plugins/harness-kit/` | builder 파생 경로 |
| archive | `ai-agent-harness-0.2.2.zip` | `harness-kit-0.3.0.zip` | 기존 archive 불변 보존 |
| repository URL | `https://github.com/hb9397/ai-agent-harness-docs` | `https://github.com/hb9397/harness-kit` | Git remote·manifest·provenance를 함께 갱신 |
| local checkout root | `D:/Dev_Workspace/ai-agent-harness-docs` | `C:/Plugin_Workspace/harness-kit` | Git `core.worktree`를 하드코딩하지 않고 checkout 기준으로 사용 |

### 1.1 이름 선정 이유

`harness-kit`을 선택한다. 짧고 kebab-case 검증 규칙에 맞으며, 현재 20개 사용자
스킬이 제공하는 설계·구현·검증·문서화 흐름을 특정 모델이나 한 가지 산출물에
가두지 않는다.

| 후보 | 판단 |
|---|---|
| `harness-kit` | **선정**. 범용적이고 설치 표면이 짧으며 확장 가능 |
| `project-harness` | 의미는 명확하지만 일반 명사 조합이라 브랜드 식별성이 약함 |
| `workflow-harness` | workflow에 치우쳐 문서·품질·스킬 포트폴리오를 충분히 표현하지 못함 |
| `agent-workbench` | IDE·실험 도구로 오해할 가능성이 있음 |

### 1.2 호환성 정책

plugin ID와 qualified install 문자열은 사용자 설치 계약이므로 이번 변경은 공개
설치 표면의 breaking change다. `0.2.2`의 manifest, archive, tag를 덮어쓰지 않고
`0.3.0`으로 승격한다. 기본 정책은 별도 legacy alias를 만들지 않는 hard cutover다.

기존 사용자는 다음 순서로 이동한다.

```text
기존 ai-agent-harness@ai-agent-harness 제거
→ marketplace hb9397 갱신
→ harness-kit@hb9397 설치
→ 새 세션에서 skill 호출 확인
```

legacy alias를 병행하면 두 plugin ID와 두 cache 경로를 계속 검증해야 하므로 이번
계획에는 포함하지 않는다. alias가 필요해지면 별도 호환성 계획으로 분리한다.

---

## 2. 완료 정의

다음 조건을 모두 만족해야 이 계획을 완료로 표시한다.

1. Codex·Claude manifest의 `name`이 모두 `harness-kit`이다.
2. Codex·Claude marketplace의 `name`이 모두 `hb9397`이고 plugin source가
   `./plugins/harness-kit`을 가리킨다.
3. 새 설치 명령과 evidence의 qualified ID가 `harness-kit@hb9397`이다.
4. 이전 `plugins/ai-agent-harness-0.2.2.zip`과 SHA-256이 변경되지 않는다.
5. `plugins/harness-kit-0.3.0.zip`과 `maintainer/plugin/release.json`이 동일한
   plugin ID·version·archive hash를 기록한다.
6. 현재 생성물·문서·검증 코드의 live reference가 새 식별자로 정합화된다.
   저장소 이름이 들어간 URL과 역사 문서는 의미를 확인해 보존한다.
7. `origin` remote가 `https://github.com/hb9397/harness-kit.git`을 가리키고,
   `main`이 `origin/main`을 추적한다.
8. live manifest·provenance·generated metadata에 이전 repository URL과
   `D:/Dev_Workspace/ai-agent-harness-docs` 절대 경로가 남지 않는다.
9. 루트 `example/`의 11개 파일(총 225,227바이트)이 외부 저장소에 복사되고,
   clone 후 파일 목록·내용·SHA-256이 일치한다.
10. 외부 이관 검증 전에는 루트 `example/`을 삭제하지 않는다.
11. 루트 `example/` 제거 후에도 `skills/**/example` 및 `skills/**/examples`의
   plugin 내부 보호 자산은 그대로 유지된다.
12. `README.md`, `AGENTS.md`, `.user-docs/**`, maintainer 문서가 새 설치명과
   외부 examples 관리 위치를 설명한다.
13. 사용자 프로젝트용 `.docs/**` 계약과 `skills/**`의 사용자 산출 경로는
   변경하지 않는다.
14. projection·plugin build·strict validation·skill eval·CLI smoke·release
   regression이 통과한다.
15. 실제 Codex App·Claude Desktop Code 모델 호출 증적이 없으면 기술 검증 성공과
   별개로 release 상태는 `not-release-ready`를 유지한다.
16. Phase별 구현·검증·커밋을 완료한 뒤에만 push·tag·GitHub Release를 별도 승인으로
   수행한다.

---

## 3. 기준선과 영향 범위

### 3.1 현재 플러그인 정본과 repository identity

이름은 다음 정본과 생성물에 중복되어 있다. 문자열 전역 치환을 하지 않고 정본을
먼저 수정한 뒤 생성기를 통해 산출물을 재생성한다.

| 계층 | 현재 위치/값 | 목표 조치 |
|---|---|---|
| source constant | `maintainer/skills/harness-plugin-maintainer/scripts/plugin_common.py`의 `PLUGIN_ID`·`PLUGIN_ROOT_REL` | `harness-kit`, 파생 root |
| builder constant | `build_plugin.py`의 `MARKETPLACE_NAME` | `hb9397` |
| repository constant | `build_plugin.py`의 `REPOSITORY_URL` | `https://github.com/hb9397/harness-kit` |
| Git remote | `.git/config`의 `remote.origin.url` | `https://github.com/hb9397/harness-kit.git` |
| manager skill 정본 | `maintainer/skills/harness-plugin-maintainer/**` | 실제 사용자 plugin ID만 새 이름으로 갱신 |
| manager projection | `.agents/skills/**`, `.claude/skills/**` | sync generator로 재생성 |
| plugin runtime | `plugins/ai-agent-harness/**` | builder로 `plugins/harness-kit/**` 재생성 |
| marketplace | `.agents/plugins/marketplace.json`, `.claude-plugin/marketplace.json` | builder 생성 결과 확인 |
| release evidence | `maintainer/plugin/{release,cli-smoke,install-verification}.*` 등 | 새 ID·qualified ID·archive 반영 |
| 사용자 문서 | `README.md`, `.user-docs/**` | 설치·호출·업데이트·제거 명령 갱신 |

현재 `0.2.2` 기준 archive SHA-256은 다음과 같이 보존한다.

```text
plugins/ai-agent-harness-0.2.2.zip
016be105eaebf46f164641946c1705edbf1b70bed7a3bf4aff661a6e0814d27c
```

현재 checkout 이동은 `.git/config`에 `core.worktree`가 없으므로 Git object와
branch history에 영향을 주지 않는다. 다만 `generated_marker()`와 internal
upstream registry의 source URL에 남은 `D:/Dev_Workspace/ai-agent-harness-docs`는
machine-specific 값이므로 새 checkout 기준에 맞추거나 stable repository URL로
정규화해야 한다. build determinism을 위해 새 `C:/Plugin_Workspace/harness-kit`
절대 경로를 다른 generated artifact에 추가하지 않는다.

### 3.2 루트 example 자산

현재 root `example/`은 다음 11개 파일이며, 플러그인 runtime에 직접 포함되는
정본이 아니다.

```text
example/README.md
example/context-doc--AGENTS.md
example/context-doc--CLAUDE.md
example/context-doc--architecture-instruction.md
example/design-doc--ACRO-BE.md
example/design-doc--ACRO-FE.md
example/design-doc--ACRO.md
example/design-prototype-docs--onboarding.md
example/impl-doc--selector-recovery.md
example/impl-fe-be-doc--ACRO.md
example/markdown-refinement-flow.md
```

현재 기준 총 크기는 225,227바이트다. `example/README.md`는 외부 운영문서와
연결되는 독립 인덱스이며, 현재 live 문서에서 root `example/`을 직접 링크하는
참조는 확인되지 않았다. 따라서 plugin runtime 동작에는 직접 영향이 없지만,
저장소 clone 사용자의 학습·수동 검증 자료가 사라지는 문서/운영 영향은 있다.

삭제하지 말아야 할 별도 자산은 다음과 같다.

```text
skills/commit/examples/**
skills/create-prototype/examples/**
skills/design-prototype-docs/example/**
```

이 경로들은 plugin payload 안에서 skill 실행이 참조하는 보호 자산이므로 root
`example/` 외부화와 함께 삭제하거나 이동하지 않는다.

### 3.3 외부 관리 대상

기본 외부 대상은 다음 별도 저장소로 계획한다.

```text
https://github.com/hb9397/ai-agent-harness-examples
```

이 저장소가 실제로 생성·쓰기 가능한 상태인지 확인하기 전에는 root 삭제 Phase를
시작하지 않는다. 외부 저장소 루트에 기존 `example/`의 파일 내용과 파일명 상대
경로를 보존하고, 이 저장소에는 외부 URL·revision·archive SHA-256을 기록한다.

### 3.4 기존 식별자 잔존 범위와 파일 분류

구현 전에 다음 패턴을 저장소 전체에서 검색하고, 각 hit를 `live`, `generated`,
`test-fixture`, `historical`로 분류한다. `ai-agent-harness`를 단순 전역 치환하지
않으며, live 항목은 새 정본으로 갱신하고 generated 항목은 generator로 재생성한다.

| 분류 | 확인·갱신 범위 | 처리 원칙 |
|---|---|---|
| 사용자 스킬 원본 | `skills/**`의 SKILL·template·prompt·eval에서 `/ai-agent-harness:`, plugin 경로·설치명 참조 | 사용자 호출 예시는 `/harness-kit:`으로 갱신하고, 사용자 프로젝트에 생성되는 `.docs`·`AGENTS.md`·`CLAUDE.md` 계약은 변경하지 않는다 |
| 관리자 스킬 정본 | `maintainer/skills/harness-plugin-maintainer/**`, `maintainer/skills/skill-portfolio-maintainer/**`의 설명·reference·script·eval·template | plugin ID, qualified ID, repository URL, User-Agent·temp prefix를 live 기준으로 갱신하고 fixture의 외부 예제 URL은 보존한다 |
| 관리자 projection | `.agents/skills/**`, `.claude/skills/**` | 직접 편집하지 않고 `sync_manager_projections.py`로 재생성·`--check` 검증한다 |
| 생성 plugin | `plugins/ai-agent-harness/**` | builder로 `plugins/harness-kit/**`를 재생성하고 runtime skill namespace·metadata·lock·NOTICE를 대조한다 |
| marketplace·설정 | `.agents/plugins/marketplace.json`, `.claude-plugin/marketplace.json`, `.gitattributes`, `.git/config`, `NOTICE` | marketplace·path·repository URL을 갱신하고 old archive path 규칙은 보존 필요성을 확인한다 |
| 사용자·관리 문서 | `README.md`, `AGENTS.md`, `.user-docs/**/*.md`, `improvement_plan/README.md`, `maintainer/README.md` | 설치·호출·관리 위치·외부 examples 링크를 갱신한다. `.user-docs`는 파일명을 열거한 목록보다 전체 glob 검색을 기준으로 누락을 방지한다 |
| release·provenance | `maintainer/plugin/**`, `maintainer/upstreams/registry.json`, `maintainer/inventory/**` | live evidence·registry·provenance는 새 ID/URL로 갱신하고 0.2.2 및 이전 audit의 historical 값은 근거와 함께 보존한다 |
| fixture·historical | `**/evals/fixtures/**`, `improvement_plan/20260729/**`, `improvement_plan/20260730/**`, `improvement_plan/20260801/**`, old archive | 테스트 재현성과 과거 release 증적을 위해 보존하되, live 검증에서 허용되는 old hit 목록에 명시한다 |

검색 기준은 다음과 같다.

```powershell
$old = 'ai-agent-harness|ai-agent-harness-docs|plugins/ai-agent-harness|ai-agent-harness@ai-agent-harness|/ai-agent-harness:|D:/Dev_Workspace/ai-agent-harness-docs'
rg -n -i --hidden --glob '!.git/**' --glob '!*.zip' --glob '!improvement_plan/**' $old .
rg -l -i --glob 'skills/**' --glob 'maintainer/skills/**' --glob '.agents/skills/**' --glob '.claude/skills/**' $old .
```

검색 결과가 live source·문서·검증 코드에 남아 있으면 Phase 6 완료로 판정하지
않는다. 단, old archive, historical plan/audit, migration 문서, 외부 upstream
fixture에 포함된 값은 분류표와 release evidence에 예외로 기록한다.

---

## 4. 범위와 비범위

### 포함

- plugin ID/name, marketplace name, display name, plugin root, archive 이름 전환
- Git `origin` remote와 live repository URL/provenance를 `hb9397/harness-kit`으로 전환
- checkout 이동으로 노출된 machine-specific 절대 경로 제거와 path-independent metadata 정리
- `0.3.0` release candidate 생성과 기존 `0.2.2` 불변성 검증
- Codex·Claude 설치/업데이트/제거 문서 및 qualified ID 갱신
- manager projection·plugin runtime·marketplace·release evidence 재생성
- root `example/`의 외부 이관, 링크·정책 갱신, 검증 후 삭제
- release regression의 root example 검사 범위 정리
- phase별 baseline, rollback, 커밋, 검증 기록

### 제외

- GitHub repository의 실제 생성·rename 작업 자체(이미 변경된 원격을 local 설정과 문서에 반영하는 것만 포함)
- 사용자 프로젝트의 `.docs/**`, `AGENTS.md`, `CLAUDE.md` 생성 계약 변경
- `skills/**` 사용자 skill 내용 및 이름 변경
- `maintainer/skills/**` 관리자 skill을 사용자 payload에 포함하는 변경
- `skills/**/example(s)`와 plugin runtime 내부 예제 제거
- `education/` 재검토·복원 또는 별도 교육자료 이관
- legacy alias 병행 운영
- 모델 호출·Desktop 수동 증적을 자동화했다는 허위 release-ready 승격

---

## 5. 전역 안전 규칙

- 이 문서 작성 시점에는 이름 변경과 root `example/` 삭제를 수행하지 않는다.
- `example/`은 보호 자산이므로 외부 clone 검증과 별도 파괴적 변경 승인 전에는
  `Remove-Item`, `git rm`, 이동을 실행하지 않는다.
- `0.2.2` archive와 `v0.2.2` tag는 immutable 기준선으로 취급한다.
- `ai-agent-harness` 문자열을 전역 치환하지 않는다. repository URL,
  historical plan, old archive, migration note인지 live plugin identity인지 파일별로
  분류한다.
- `AGENTS.md`와 `.user-docs/**`가 새 경로·설치명을 설명하기 전에는 root 제거를
  완료로 표시하지 않는다.
- `origin` URL은 `https://hb9397@github.com/hb9397/harness-kit.git`로 확인할 수
  있지만 token을 URL에 저장하지 않는다. `user.name`·`user.email`은 commit identity와
  push authentication을 혼동하지 않도록 별도로 점검한다.
- `core.worktree`·hook·include 설정이 새 checkout을 절대 경로로 묶고 있지 않은지
  확인한다.
- 생성물은 `sync_manager_projections.py`, `build_plugin.py`, 검증기에서만 갱신한다.
- 각 Phase는 구현·검증·커밋을 한 묶음으로 완료한다. push·tag·release는 마지막
  별도 승인 전까지 수행하지 않는다.

---

## 6. 구현 Phase

### Phase 0 — 기준선·inventory·변경 허용 목록 고정

**목표**: `0.2.2`, plugin identity, root `example/` 보호 자산, 현재 live reference를
수치와 hash로 고정한다.

작업:

1. `git status --short --branch`, `git rev-parse HEAD`, `git show-ref --tags v0.2.2`로
   작업 트리와 tag를 기록한다.
2. `git remote -v`, `git config --local --get-regexp '^(remote\\.|branch\\.|core\\.|include\\.)'`,
   `git rev-parse --show-toplevel`로 새 checkout·remote·tracking을 기록한다.
3. `git ls-files example`와 파일별 SHA-256을 기록한다.
4. `maintainer/inventory/example-externalization-baseline.json`을 새로 만들고
   파일 목록·바이트 수·SHA-256·기준 commit을 기록한다.
5. `ai-agent-harness` hit를 live identity, repository URL, historical record,
   old release artifact, migration note로 분류한다.
6. `.docs/**`와 skill-internal `example(s)`가 변경 대상에서 제외되었는지 확인한다.

검증:

```powershell
git status --short --branch
git rev-parse HEAD
git show-ref --tags v0.2.2
git remote -v
git rev-parse --show-toplevel
git ls-files example
Get-ChildItem -LiteralPath 'example' -Recurse -File |
  Get-FileHash -Algorithm SHA256
git diff --check
git diff --cached --check
```

완료 기준: clean worktree, `origin`이 새 repository URL을 가리킴, root example
11개/225,227바이트, 기존 archive hash 일치, baseline JSON 검토 완료.

### Phase 1 — 외부 examples 저장소 이관과 복구 검증

**목표**: 삭제 전에 외부 저장소에서 동일한 운영 자산을 복구할 수 있음을 증명한다.

작업:

1. `hb9397/ai-agent-harness-examples` 저장소 생성·접근 권한·기본 branch를 확인한다.
2. root `example/`의 내용을 외부 저장소 루트로 복사해 파일명 상대 경로를 보존한다.
3. 외부 저장소에서 commit/tag를 만들고 revision을 기록한다.
4. 별도 임시 디렉터리로 clone해 파일 목록·내용·SHA-256을 baseline과 대조한다.
5. 외부 README에 원 저장소와 plugin `harness-kit`의 관계, 예제의 비실행·참고용
   성격, license/NOTICE를 기록한다.
6. 이 저장소의 inventory에 외부 URL·revision·verified_at·archive hash를 기록한다.

중단 기준: 외부 저장소 생성 실패, 권한 불명확, clone 비교 불일치, license/NOTICE
정합성 실패 중 하나라도 있으면 root `example/` 삭제로 진행하지 않는다.

### Phase 2 — 정책·문서·release regression 범위 갱신

**목표**: root example이 외부 관리로 이동한 사실과 보호 자산 경계를 현행 문서와
검증 코드에 반영한다.

작업:

1. `AGENTS.md`의 문서 동반 갱신·보호 자산 규칙을 외부 examples 저장소 기준으로
   갱신하고, root 삭제 후에도 내부 `skills/**/example(s)` 보호 규칙을 남긴다.
2. `README.md`, `AGENTS.md`, `.user-docs/**/*.md`, `maintainer/README.md`,
   `improvement_plan/README.md`의 외부 examples 링크·관리 책임·보호 자산
   경계를 갱신한다. 개별 파일을 열거하는 것보다 전체 `.user-docs` glob을
   검색해 `Agent_Skills_Repo_Structure_Analysis.md`,
   `Imported_Skill_Provenance.md` 같은 보조 문서의 live 참조도 누락하지 않는다.
3. root `example/README.md`의 내용을 외부 README로 옮겼는지 확인하고, 현재
   저장소에는 존재하지 않는 local link를 새로 만들지 않는다.
4. `run_release_regression.py`의 local link 검사에서 제거될 root `example/`을
   더 이상 필수 local root로 열거하지 않는다. `.user-docs`와
   `improvement_plan`만 local 문서 검사 대상으로 남긴다.
5. external URL은 local-link checker가 remote URL로 건너뛰도록 Markdown 형식을
   유지한다.

검증:

```powershell
rg -n -i 'example/|examples/' README.md AGENTS.md .user-docs maintainer
python maintainer/skills/harness-plugin-maintainer/scripts/run_release_regression.py
```

### Phase 3 — plugin identity source 변경

**목표**: 이름을 정본 상수에서 한 번만 바꾸고 생성물에 전파할 수 있게 한다.

작업:

1. `plugin_common.py`의 `PLUGIN_ID`를 `harness-kit`으로 변경하고
   `PLUGIN_ROOT_REL`이 `plugins/harness-kit`을 파생하도록 유지한다.
2. `build_plugin.py`의 `MARKETPLACE_NAME`을 `hb9397`로 변경한다.
3. manifest의 `displayName`과 설명을 `Harness Kit` 중심으로 갱신한다.
4. builder·validator·eval의 `plugins/ai-agent-harness` 하드코딩을
   `PLUGIN_ROOT_REL` 또는 새 정본 경로로 바꾼다.
5. `skills/**`의 SKILL·template·prompt·eval에 있는 `/ai-agent-harness:` 호출
   예시와 identity-specific plugin 경로·검증 기대값을 새 namespace로 갱신한다.
   사용자 프로젝트에 생성되는 `.docs`·`AGENTS.md`·`CLAUDE.md` 계약 자체는
   변경하지 않는다.
6. `maintainer/skills/harness-plugin-maintainer/**`와
   `maintainer/skills/skill-portfolio-maintainer/**`의 live 설명·reference·script·
   eval·template에서 실제 plugin identity, repository URL, User-Agent·temp
   prefix를 갱신한다. 역사 문서·old release artifact·migration note와 외부
   upstream fixture는 의미를 확인해 보존한다.
7. `.agents/skills/**`, `.claude/skills/**`는 직접 편집하지 않고 다음 Phase에서
   projection generator로 갱신한다.
8. `maintainer/upstreams/registry.json`의 internal source URL을 machine-specific
   checkout path가 아닌 `https://github.com/hb9397/harness-kit` 기준으로 정규화한다.
9. `plugin_common.py`의 `generated_marker()`에서 절대 로컬 경로가 build 결과에
   들어가지 않도록 stable repository identity 또는 path-independent marker를 사용한다.

검증:

```powershell
rg -n -i 'PLUGIN_ID|MARKETPLACE_NAME|REPOSITORY_URL|plugins/ai-agent-harness|ai-agent-harness@ai-agent-harness|/ai-agent-harness:' skills maintainer/skills README.md AGENTS.md .user-docs maintainer/plugin .agents/plugins .claude-plugin
git remote get-url origin
git rev-parse --show-toplevel
```

허용되는 old hit는 old archive, historical record, migration note로
분리해 기록한다. live install identity에는 old qualified ID가 남지 않아야 한다.

### Phase 4 — plugin·projection·metadata 재생성

**목표**: 새 ID와 marketplace namespace로 결정적 plugin artifact를 생성한다.

작업:

1. builder가 새 `plugins/harness-kit/`를 생성하도록 실행한다.
2. 기존 generated `plugins/ai-agent-harness/` tree는 새 tree와 내용·manifest를
   대조한 뒤 제거한다. 단, `plugins/ai-agent-harness-0.2.2.zip`은 보존한다.
3. `.agents/plugins/marketplace.json`과 `.claude-plugin/marketplace.json`을
   builder로 재생성한다.
4. manager projection을 동기화한다.
5. `harness-kit-0.3.0.zip`, `release.json`, manifest, capability, upstream lock,
   manifest hash를 생성한다.
6. plugin payload 내부 skill 수 20, agent 수 0, internal examples 보존을 확인한다.

새 manifest와 NOTICE의 homepage/repository/website URL은 모두
`https://github.com/hb9397/harness-kit`이어야 한다. local marketplace source는
repository root에 상대적인 `./plugins/harness-kit`만 사용한다.

명령:

```powershell
python maintainer/skills/harness-plugin-maintainer/scripts/build_plugin.py
python maintainer/skills/harness-plugin-maintainer/scripts/sync_manager_projections.py
```

### Phase 5 — 설치 문서·migration·evidence 갱신

**목표**: 사용자가 새 설치명으로 이동할 수 있고, 증적이 실제 명령과 일치하게 한다.

작업:

1. `README.md`와 `.user-docs/Plugin_Installation_Guide.md`의 Codex 명령을
   `harness-kit@hb9397` 기준으로 갱신한다.
2. Claude Code CLI·Desktop의 `/harness-kit:skill-name` namespace 예시를 갱신한다.
3. upgrade/remove/install 명령과 migration 절차에 old qualified ID를 명시하되,
   신규 기본 명령에는 사용하지 않는다.
4. `.user-docs/**/*.md`, maintainer README, release checklist, phase audit,
   `cli-smoke.json`, `install-verification.json`, `release.json` 및 기타
   `maintainer/plugin/**` evidence의 live plugin ID, marketplace, archive,
   skill 수를 갱신한다. `.user-docs` 보조 문서와 provenance 문서도 전수 검색
   결과에 따라 함께 갱신하고, historical 값은 old-release 예외로 표시한다.
5. `improvement_plan/README.md`와 이 계획서에는 변경 사유·기준 tag·새 설치명을
   기록하고, 적용 대상 checkout을 `C:/Plugin_Workspace/harness-kit`으로 표기한다.
6. local marketplace를 사용한 기존 Codex·Claude 설치 증적은 old absolute path가
   아니라 새 local root 또는 GitHub repository source를 사용하도록 갱신한다.

### Phase 6 — 통합 검증

**목표**: package identity 변경과 root example 제거가 plugin 동작을 깨지 않았음을
자동화된 검증으로 증명한다.

필수 검증:

```powershell
python maintainer/skills/skill-portfolio-maintainer/scripts/validate_registry.py
python maintainer/skills/harness-plugin-maintainer/scripts/build_plugin.py --check
python maintainer/skills/harness-plugin-maintainer/scripts/validate_plugin.py
python maintainer/skills/harness-plugin-maintainer/scripts/run_all_skill_evals.py
python maintainer/skills/harness-plugin-maintainer/scripts/sync_manager_projections.py --check
python maintainer/skills/harness-plugin-maintainer/scripts/freeze_manager_inventory.py --check
python maintainer/skills/harness-plugin-maintainer/scripts/verify_install_surfaces.py --check
python maintainer/skills/harness-plugin-maintainer/scripts/run_release_regression.py
```

Codex·Claude strict 검증과 Windows `.cmd` explicit CLI smoke에서는 다음을 확인한다.

- marketplace `hb9397` 등록
- `harness-kit@hb9397` 설치·목록·cache inventory
- Codex·Claude 각각 20 skills / 0 agents
- uninstall/remove와 임시 설정 정리
- internal skill examples 보존
- old `0.2.2` archive hash 불변
- new `0.3.0` archive hash와 release metadata 일치
- `.docs/**` diff 없음
- `git remote get-url origin`이 `https://github.com/hb9397/harness-kit.git`을 반환
- live tracked files에 이전 GitHub URL과 `D:/Dev_Workspace/ai-agent-harness-docs`가 없음
- root `example/` local link scope 제거 후 missing link 0건

### Phase 7 — Phase 커밋과 release gate

권장 커밋 분리:

1. `docs(plan): add harness-kit identity and example externalization plan`
2. `chore(examples): externalize protected examples and update policy`
3. `refactor(plugin): rename plugin identity to harness-kit`
4. `docs(release): migrate install commands to harness-kit@hb9397`
5. `test(plugin): verify harness-kit 0.3.0 release candidate`

각 커밋 직전에 `git diff --check`, staged diff 검토, generated drift 검사를 수행한다.
모든 자동 검증과 필요한 수동 증적을 확보한 뒤에만 별도 승인으로 `main` push,
`v0.3.0` tag, GitHub Release를 수행한다. 모델·Desktop 증적이 없으면 tag를 만들 수
있더라도 release metadata는 `not-release-ready`를 유지한다.

---

## 7. 위험·rollback

| 위험 | 조기 신호 | 대응 |
|---|---|---|
| 외부 examples 이관 불완전 | clone hash mismatch | root 삭제 중단, 외부 commit 보완 |
| remote 설정 잔존 | `origin`이 old repository URL을 반환 | push 전 `git remote set-url origin`과 fetch 검증 |
| machine-specific path 잔존 | 다른 checkout에서 generated drift 또는 provenance mismatch | stable URL/relative marker로 정규화 |
| old plugin ID 잔존 | live docs/evidence에 old qualified ID 발견 | old hit 분류 후 live 파일만 정정 |
| generated path drift | build `--check` diff | source constant 수정 후 projection/plugin 재생성 |
| 기존 사용자 cache 충돌 | old/new plugin이 동시에 목록에 노출 | migration 문서와 remove→install 순서 검증 |
| 내부 skill example 손실 | plugin manifest에서 internal path 누락 | root example 삭제를 되돌리고 builder 입력 확인 |
| `.docs` 계약 변경 | `git diff -- .docs` 또는 관련 template diff | 해당 변경 즉시 제거; 사용자 산출 경로는 범위 밖 |
| Desktop/model 증적 부재 | release gate `not-release-ready` | 기술 검증과 readiness를 분리해 상태 유지 |

rollback은 `v0.2.2` tag와 보존 archive를 기준으로 한다. 새 `0.3.0` artifact를
삭제하거나 old tag를 force-update하지 않는다. 외부 examples 저장소 이관이 실패하면
root `example/`은 원래 위치에 남긴다.

---

## 8. 최종 체크리스트

### 기준선·externalization

- [ ] `a357492`와 `v0.2.2`가 기록됐다.
- [ ] local root가 `C:/Plugin_Workspace/harness-kit`으로 확인됐다.
- [ ] `origin`이 `https://github.com/hb9397/harness-kit.git`을 가리킨다.
- [ ] `main`의 upstream이 `origin/main`이고 fetch가 성공했다.
- [ ] root `example/` 11개 파일과 225,227바이트 inventory가 기록됐다.
- [ ] 외부 저장소 `hb9397/ai-agent-harness-examples`의 revision·hash가 기록됐다.
- [ ] clone 복구 비교가 통과했다.
- [ ] 외부 검증 전 root `example/`을 삭제하지 않았다.

### identity migration

- [ ] plugin ID는 `harness-kit`이다.
- [ ] marketplace name은 `hb9397`이다.
- [ ] qualified install은 `harness-kit@hb9397`이다.
- [ ] plugin root는 `plugins/harness-kit`이다.
- [ ] archive는 `harness-kit-0.3.0.zip`이다.
- [ ] `ai-agent-harness-0.2.2.zip`과 SHA-256은 불변이다.
- [ ] live 문서·evidence·검증 코드의 old qualified ID가 제거됐다.
- [ ] live repository URL은 `hb9397/harness-kit`이고 historical record는 의미에 맞게 보존됐다.
- [ ] generated metadata와 registry에 old local absolute path가 남지 않았다.

### 보호 자산·문서

- [ ] `AGENTS.md`와 `.user-docs/**`가 외부 examples 위치를 설명한다.
- [ ] root `example/`이 삭제된 뒤 내부 skill `example(s)`는 모두 남아 있다.
- [ ] `.docs/**`와 사용자 skill 산출 경로는 변경되지 않았다.

### 검증·release

- [ ] build, strict validate, registry, eval, projection, freeze가 통과했다.
- [ ] Codex·Claude CLI smoke의 설치 식별자가 `harness-kit@hb9397`이다.
- [ ] release regression과 local link 검사가 통과했다.
- [ ] 모델/App/Desktop 수동 증적 유무에 따라 `release-ready`를 정직하게 판정했다.
- [ ] 각 Phase의 구현·검증·커밋이 완료됐다.
- [ ] push·tag·GitHub Release는 마지막 별도 승인 후에만 수행됐다.
