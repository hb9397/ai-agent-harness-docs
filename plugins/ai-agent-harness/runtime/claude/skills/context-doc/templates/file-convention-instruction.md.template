# file-convention-instruction.md — 파일 생성 규칙

> [프로젝트명]의 파일 위치·네이밍·디렉토리 추가 기준

---

## 폴더별 파일 배치 기준

<!-- 새 파일을 어느 폴더에 넣어야 하는지 기준 -->

```
[폴더명]/     → [이 폴더에 넣는 파일의 기준]
[폴더명]/     → [이 폴더에 넣는 파일의 기준]
[폴더명]/     → [이 폴더에 넣는 파일의 기준]
```

---

## 파일 네이밍 컨벤션

<!-- 파일명 케이스, 접미사/접두사 규칙 -->

- [규칙]

---

## 한 파일에 담는 기준

<!-- 파일당 1 class, 1 컴포넌트 등 -->

- [규칙]

---

## 진입점/특수 파일 규칙

<!-- main.py, app.ts, index.ts 등에 쓰면 안 되는 것 -->

| 파일 | 담을 수 있는 것 | 담으면 안 되는 것 |
|------|----------------|------------------|
| | | |

---

## 새 폴더 추가 기준

<!-- 어느 시점에 새 폴더를 만들 수 있는가 -->

- [기준]

---

## 금지 목록

| 금지 패턴 | 이유 | 대안 |
|----------|------|------|
| | | |
