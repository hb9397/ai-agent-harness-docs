# architecture-instruction.md — 아키텍처 제약

> [프로젝트명]의 모듈·레이어 경계, 의존성 방향, 책임 분리 규칙

---

## 모듈/레이어 책임

<!--
각 모듈/레이어가 무엇을 담당하고, 무엇을 담당하지 않는지 표로 명시.
"금지" 컬럼에는 해당 모듈이 절대 해서는 안 되는 일을 적는다.
-->

| 모듈/레이어 | 책임 | 금지 |
|------------|------|------|
| `[폴더명]/` | [담당 책임] | [금지 책임] |
| `[폴더명]/` | [담당 책임] | [금지 책임] |

---

## 의존성 방향

<!--
어느 레이어가 어느 레이어를 호출할 수 있는가.
화살표 다이어그램 또는 문장 규칙으로 표현.
-->

- **[레이어 A]** → **[레이어 B]**: 허용
- **[레이어 B]** → **[레이어 A]**: 금지

위반 시:
1. 위반 내용 명시
2. 올바른 레이어/모듈로 이동시킨 코드 제안

---

## 집중 관리 모듈

<!--
직접 호출 금지 + 반드시 이 모듈을 통해서만 접근해야 하는 것들.
예: API 클라이언트, DB 접근 레이어, 공통 훅 등.
-->

| 집중 관리 대상 | 파일 경로 | 직접 호출 금지 대상 |
|--------------|----------|-------------------|
| | | |

---

## 진입점 파일의 책임 범위

<!--
main.py, app.ts, index.ts 등 진입점 파일에 작성 가능한 코드 / 금지 코드.
-->

- **[진입점 파일]에 작성 가능**: [내용]
- **[진입점 파일]에 금지**: [내용]

---

## 금지 목록

| 금지 패턴 | 이유 | 대안 |
|----------|------|------|
| | | |
